import Vue from 'vue'
import util from '../util.js';

describe('sendConfirm.vue', () => {
    it('login fixed', () => {
        console.error(Object.keys(expect(1)))
        expect(1).to.be.equal(1);
    });
})
